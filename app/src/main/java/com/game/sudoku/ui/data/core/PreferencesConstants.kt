package com.game.sudoku.ui.data.core

class PreferencesConstants {

    companion object {
        val DEFAULT_SHOW_TIMER = true
        const val DEFAULT_HIGHLIGHT_MISTAKES = 1
        const val DEFAULT_MISTAKES_LIMIT = false

        const val DEFAULT_DARK_THEME = 0
    }

}
